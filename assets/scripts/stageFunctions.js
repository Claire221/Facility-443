// Countdown Function to when guards know your missing

function countDown () {
    // set timer to 30?

    // have the timer slowly count down

    // take away from timer for actions that take a long time

    // once timer hits 0 have a window pop up that says prisoner has escaped
    
    // roll? to see if player will be caught by guards
    // if player is caught screen pops up with text
}